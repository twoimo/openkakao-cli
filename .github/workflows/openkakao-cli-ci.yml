name: openkakao-cli CI

on:
  push:
    paths:
      - "src/**"
      - "tests/**"
      - "scripts/**"
      - "examples/launchd/**"
      - "docs/**"
      - "config.example.toml"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - ".github/workflows/openkakao-cli-ci.yml"
  pull_request:
    paths:
      - "src/**"
      - "tests/**"
      - "scripts/**"
      - "examples/launchd/**"
      - "docs/**"
      - "config.example.toml"
      - "Cargo.toml"
      - "Cargo.lock"
      - "rust-toolchain.toml"
      - ".github/workflows/openkakao-cli-ci.yml"

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  MANIFEST: Cargo.toml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""

      - name: Cache cargo registry and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-test-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: cargo-test-${{ runner.os }}-

      - name: Cargo test
        run: cargo test --manifest-path $MANIFEST

      - name: Fake-only Python AutoReply tests
        run: >-
          python3 -m unittest
          tests.test_auto_reply_cli_runtime
          tests.test_auto_reply_service_entry
          tests.test_auto_reply_session_monitor
          tests.test_auto_reply_session_packager
          tests.test_auto_reply_tui

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""

      - name: Cache cargo registry and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-lint-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: cargo-lint-${{ runner.os }}-

      - name: Cargo fmt
        run: cargo fmt --manifest-path $MANIFEST --check

      - name: Cargo clippy
        run: cargo clippy --all-targets --manifest-path $MANIFEST -- -D warnings

  launchd-artifacts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Launchd artifact harness
        run: sh scripts/test-auto-reply-launchd-artifacts.sh

      - name: Fake-only Python AutoReply tests
        run: >-
          python3 -m unittest
          tests.test_auto_reply_cli_runtime
          tests.test_auto_reply_service_entry
          tests.test_auto_reply_session_monitor
          tests.test_auto_reply_session_packager
          tests.test_auto_reply_tui

  macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""

      - name: Cache cargo registry and build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-macos-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: cargo-macos-${{ runner.os }}-

      - name: Cargo test
        run: cargo test --manifest-path $MANIFEST

      - name: Launchd artifact harness
        run: sh scripts/test-auto-reply-launchd-artifacts.sh

      - name: Build release
        run: cargo build --release --manifest-path $MANIFEST

      - name: Smoke test
        run: ./target/release/openkakao-cli --version
